# Analytics Events — Everything Bundle

- `product_view` -> trigger: page_view on /products/everything-bundle/
- `add_to_cart` -> trigger: click on [Get Everything Bundle] CTA
- `purchase` -> trigger: checkout success webhook for SKU SS-025
- `download_complete` -> trigger: secure link file served

GTM trigger names:
- GTM_ProductView_025
- GTM_AddToCart_025
- GTM_Purchase_025
- GTM_DownloadComplete_025
