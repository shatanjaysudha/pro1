# Analytics Events — Resource Library Access (Membership)

- `product_view` -> trigger: page_view on /products/resource-library-membership/
- `add_to_cart` -> trigger: click on [Get Resource Library Access (Membership)] CTA
- `purchase` -> trigger: checkout success webhook for SKU SS-024
- `download_complete` -> trigger: secure link file served

GTM trigger names:
- GTM_ProductView_024
- GTM_AddToCart_024
- GTM_Purchase_024
- GTM_DownloadComplete_024
