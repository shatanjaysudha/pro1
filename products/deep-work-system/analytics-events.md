# Analytics Events — Deep Work System

- `product_view` -> trigger: page_view on /products/deep-work-system/
- `add_to_cart` -> trigger: click on [Get Deep Work System] CTA
- `purchase` -> trigger: checkout success webhook for SKU SS-002
- `download_complete` -> trigger: secure link file served

GTM trigger names:
- GTM_ProductView_002
- GTM_AddToCart_002
- GTM_Purchase_002
- GTM_DownloadComplete_002
